export interface IHeroSection {
  withContent?: boolean;
  withSearch?: boolean;
}

export interface IHeroContentProps {
  title: string;
  description: string;
  buttonText: string;
  buttonLink: string;
}
